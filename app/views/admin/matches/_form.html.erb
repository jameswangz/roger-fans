<%= form_for(@admin_match) do |f| %>
  <% if @admin_match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_match.errors.count, "error") %> prohibited this admin_match from being saved:</h2>

      <ul>
      <% @admin_match.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ordinal %><br>
    <%= f.number_field :ordinal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
